# Carry プロジェクト — 作業ルール

ゴブリンが壺いっぱいのポーションを運んで魔王城を目指す3Dアクションゲーム(Unity)。
ステージ全体構成の基準資料: `Reference/Stage/stage_overview.png`

## 担当範囲

- Unityでのステージ制作、Blenderでの3Dアセット制作・修正、外部アセット導入、ステージギミック実装、ステージ調整。
- キャラクターモデル・リギング・モーション制作は別担当。ここでは触らない。

## 外部アセットのルール

- 無料・商用利用可能・改変可能・ライセンスが明確に確認できるアセットのみ使用する。
- 出所不明・ライセンス不明なアセットはプロジェクトに追加しない。
- 使用した外部アセットは必ず `ASSET_LICENSES.md` に記録する(作者・配布元・URL・ライセンス・商用利用可否・クレジット表記要否・改変可否・格納場所・使用箇所)。
- 一つのアセットパックで完結させる必要はない。複数の無料・商用利用可能アセットを組み合わせてよい。見た目の統一感はマテリアル・色・スケール・ライティングの調整で揃える。
- 既存の高品質な環境アセットを「土台」として導入し、大幅に改変してゲーム専用ステージへ作り替えるアプローチを優先する。そのまま使うことが目的ではない。

## Unityシーン編集の作業方法(2026-08-14方針変更: MCP優先)

- Unityとの連携は **MCP for Unity(`UnityMCP` MCPサーバー、HTTP `http://127.0.0.1:8080/mcp`)経由を基本とする**。作業開始時にUnity Editorでこのプロジェクトを開いた状態にし、`claude mcp list` で `UnityMCP` が `✔ Connected` になっていることを確認してから着手する。未接続の場合はUnity Editorの「MCP For Unity」ウィンドウを開き、「Configure」でClaude Codeクライアントとして再登録する。
  - `UnityMCP` はローカルスコープ(`claude mcp add` によりこのマシン・このプロジェクトに私的登録)なので、別のマシン/別ユーザーでは各自セットアップが必要。`.mcp.json`(コミット対象)には含めない。
  - MCPサーバーの登録・解除はセッション起動後には反映されないため、新規登録/変更した場合は一度Claude Codeセッションを再起動してツールを読み込み直す。
- 通常のシーン参照・GameObject操作・コンポーネント設定などは、MCPツール(`mcp__UnityMCP__*`)を使ってUnity Editor上で直接行ってよい。
- ただし再現性が必要な大規模作業(ステージ全体構築、地形生成、大量オブジェクトの一括配置など)は、その場限りの手作業で終わらせず、引き続き `Assets/Editor/` 配下のEditorスクリプトとして実装し、MCP経由でそのメソッドを実行する形にする。スクリプトとして残すことで、何度でも同じ結果に作り直せる状態を保つ。
- 大きな方針転換でステージを作り直す場合は、既存シーンを壊さず、必ずバックアップ(別名保存 or 新規シーン)してから作業する。
- 見た目の確認は、MCP接続時はUnity Editorのゲームビューを直接参照できるが、判断基準は従来通りゲームカメラ(プレイヤー視点付近)からの見え方とする。SceneビューやTop-downで「森に見える」ことを完成の判断基準にしない。
- **フォールバック**: Unity EditorがMCP経由で操作不能な場合(Editor未起動、`UnityMCP` が `Connected` にならない等)に限り、従来のバッチモード起動(`-batchmode -quit -executeMethod`)を使う。バッチモード実行前に、対象プロジェクトが既にUnity Editorで開かれていないか確認する(開かれていると `-batchmode` は失敗する)。バッチモードでは画面を目視できないため、`Camera.Render()` を使ったスクリーンショット撮影ツール(`Assets/Editor/CarryCapture*.cs`)で確認する。バッチモードの初回レンダリングはシェーダー/テクスチャの読み込み未完了で色化けすることがあるため、本番カット前に数回ウォームアップレンダリングを挟む。

## 既知の落とし穴

- 一部の外部FBXはUnityの自動マテリアル生成でテクスチャがリンクされず白/水色に表示される。`ModelImporter.materialLocation = External` で実体化してから `_BaseMap` を手動で貼り直す。
- 一部のFBXはスケール単位が他アセットと異なり、インポート時に極端に小さく(または大きく)なることがある。実寸を確認し、配置スクリプト側で補正スケールをかける。
- 見た目の確認は必ずゲームカメラ(プレイヤー視点付近)から行う。SceneビューやTop-downで「森に見える」ことを完成の判断基準にしない。
- `Terrain.SampleHeight(worldPos)` は **Terrain GameObjectのローカル高さ(0〜TerrainData.size.y)を返し、Y方向のtransform.position(ワールド座標オフセット)を含まない**。ワールド座標として使うには `terrain.transform.position.y` を必ず加算すること(このプロジェクトでは `CarryBuildTerrainForest.SampleWorldHeight()` ヘルパーを使う)。この加算漏れが、長らく原因不明だった「岩やアイテムが地形の上空に浮遊する」バグの実際の原因だった可能性が高い(2026-08-11に発覚・修正)。地形の原点Yが0に近い間は誤差が目立たず気づきにくいので注意。
- 手続き生成メッシュ(リボン/ストリップ状のメッシュ)を三角形インデックスで組む際は、法線の向き(巻き順)を必ず検算する。巻き順を間違えると面自体は描画される(特にCull Offマテリアルでは見た目に破綻がなく気づきにくい)が、法線が裏向きになりライティングが不自然に暗くなる。上向き/外向きにしたい面ごとに `(頂点A, 頂点B, 頂点C)` の外積の符号を手計算するか、テストレンダリングで暗く見えていないか確認すること。

## Rock / Cliff / Boulder / Tree の接地ルール(2026-08-14制定、必須)

Terrain・崖・別の岩に接触しているべきオブジェクトを、見た目の当てずっぽうでTransformを設定して配置することを禁止する。「岩壁がTerrainから浮いている」問題がこれまで繰り返し発生した根本原因は、実際のSurfaceを調べずに(a) Lake中心からの放射方向を「outward」とみなして回転を決め、(b) 狭い範囲の高さサンプルだけで接地Y座標を決めていたこと。今後は以下を標準手順とする(`CarryBuildTerrainForest.cs` の `TryGetTerrainSurface()` / `PlaceCliffEmbedded()` / `PlaceBoulderEmbedded()` を参照・再利用すること)。

1. **Raycastで実際のSurfaceを取得する**: `TerrainCollider.Raycast()`(または対象が別の岩の場合は`Physics.Raycast`)で `hit.point` と `hit.normal` を取得する。放射方向や決め打ちの向きで代用しない。
2. **回転はhit.normalを基準にする**: 大型Cliff/Rock Face(直立した壁として見せたいアセット)は、normalを水平面に投影してYaw方向のみ合わせ、直立姿勢を保つ。中小型のBoulder/岩は、normalへの完全なアライメント(Pitch/Rollも含む)で局所的な斜面へ自然になじませてよい。
3. **Boundsを必ず確認する**: Pivot位置だけで接地判定しない。`Renderer.bounds` / メッシュのローカルBounds(`GetPrefabBottomLocalY`等の既存ヘルパー参照)を使い、モデルの実際の奥行き・高さを考慮してオフセットを計算する。
4. **背面をTerrain内部へ埋め込む**: 岩をSurfaceへ置いた後、hit.normalと逆方向へ「モデル奥行き × embedFrac」分だけ押し込み、背面がTerrain内部に自然に埋没するようにする。前面だけがSurfaceから露出する状態を作る。
5. **横に広いアセットは複数点サンプリングする**: 幅が数十mある帯状のCliffアセットを1点のRaycast/高さサンプルだけで接地させない。幅方向に複数点をサンプリングし、最も低い点を基準に埋め込み深さを決める(でなければ端が浮く)。
6. **浮遊検証を配置後に必ず行う**: 各オブジェクトについて、接地予定方向へRaycastし、Terrainまたは隣接岩との間に不自然な隙間がないか確認する(`CarryTempGroundingDiag.cs` のパターンを再利用・拡張する)。
7. **隙間は岩を追加して隠さない**: 隙間が見つかった場合、まず既存オブジェクトの位置・回転・スケール・Terrain形状を調整して解決する。新しい岩を足して隠すのは最後の手段。岩を継続的に追加すると過去に問題になった「土砂崩れのような景観」に戻ってしまう。
8. **崖の基本構造はTerrainで作る**: 崖は「Terrain(基本形状)→大型Cliff/Rock Face(Terrainを覆う装飾)→中型岩→小型岩→苔/蔦/シダ」という階層で構築する。崖そのものを大量の小型岩の集合体として作らない。

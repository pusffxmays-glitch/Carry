# OPEN ISSUES — ポーション液体システム

未解決の課題をここに集約する。フェーズを先へ進める判断をした場合でも、
ここに残っている限り「直っていない」という扱いにする。

---

## OI-1: リム開口に残る堰 — 傾けても液体が半分しか出ない

**状態**: 未解決 / 原因の性質までは特定済み
**発見**: Phase 7（2026-08-13）
**影響度**: **Phase 8 の合格条件をブロックする。ただしゲームプレイへの影響は限定的。**

Phase 11 の実測で、ゲーム中の壺の傾きは `armBalance` ±1.0 で **±10 度が上限**と分かった。
45% 充填の壺は 10 度ではこぼれないので、実際の「こぼれ」は
よろけ (GoblinStagger) による揺さぶりで起きており、そちらは正しく動いている。
つまり OI-1 が塞いでいるのは「傾け続けて注ぐ」挙動と TEST K/L であって、
現状のゲームループそのものではない。
当初は「見た目の破綻ではないので後工程は続行可能」と判断したが、Phase 8 の着手で
**TEST K（液だれ）/ TEST L（液滴）が OI-1 のせいで成立しない**ことが分かった。

- 55° 傾斜（Drip ケース）: 液体はリムに張り付いたまま**落ちない**。ground 0 個。
- 62° まで 1.8 秒傾けてから戻す（DripBack ケース）: **16384 個中 16384 個が壺内に戻った**。
  リムに残る液が無いので、Neck も液滴も発生しようがない。
- 62° 保持（Pour ケース）: 逆に太いシート状の流れになり、これも Neck / 液滴にならない。

つまり「少量の液体がリムを越えて細く垂れる」という状態が作れない。
**TEST K / L を合格させるには OI-1 の解決が先。**

### 症状

壺を傾けて保持すると、液面が**最低リム点より 0.12〜0.14 m 高いところで静止**し、
それ以上流れ出さない。40 秒保持しても頭打ち。

| 傾き | 実測の残存 | 理論の残存 |
|---|---|---|
| 62° | 79〜83% | 41.7% |
| 80° | 36.8% | 12.4% |
| 100° | 11.8% | 0.2% |

理論値は「最低リム点を通る水平面より下の壺内容積」から算出（体積重み付き
モンテカルロ、`_p7j.cs`）。

### 測定の妥当性（誤測定ではないことの確認済み）

- 「壺内粒子数 x 粒子体積」から逆算した液面 **1.073** と、粒子分布から見た液面
  **1.079** が一致。本当に堰より上まで液体が詰まっている。
- バルクは完全に静定している（堰-0.15m 以下で平均 0.019 m/s、rho/rho0 = 1.019）。
  暴れて盛り上がっているのではなく、**静的な平衡**として堰が成立している。
- 液面は world 水平（world Z で 12 分割して上面を測り、全区間 1.094 +/- 0.01）。
  重力の向きは正しい。

### 原因として否定できたもの（すべて実測）

| 仮説 | 検証 | 結果 |
|---|---|---|
| 表面張力が液膜を張っている | cohesion/curvature = 0 | 不変（84.2% → 84.9%） |
| Akinci psi が開口端で発散 | edgeVolumeClamp 1.25 / 1.02 | 不変 |
| SafetyCorrection がリムで引き戻す | SafetyTopY でリム帯を除外 | 不変 |
| リム上端の外側シェルが棚になる | 上端をテーパー | 不変 |
| Sim 領域の壁 | 領域を地面まで拡大 | 不変（別のバグは直った） |
| 境界粘性が流出層を壁に貼り付ける | dt 比例化 + bv 0〜4 | **90% → 78% まで改善。ただし下げ止まる** |
| 圧力ソルバーが弱い | solverIterations 4 → 10 | 不変（78.6% → 78.3%） |
| カーネル半径スケールの離散化 | krs 2.0 / 1.7 / 1.5 | 堰 0.140 / 0.124 / 0.123 m。**h に比例しない** |
| 人工圧力 | artificialPressureFraction = 0 | リムの暴れは 2.15 → 0.89 m/s に減るが**堰は不変** |
| 自由表面の lambda 下限 | minDenomFraction 0.5 → 1.0 | 不変 |

### 分かっている性質

**堰の高さは `boundaryPressureScale` に比例する。**

| boundaryPressureScale | 液面 - 堰 | 壁の貫通 |
|---|---|---|
| 1.0 | 0.124 m | 465 |
| 1.6（採用値） | 0.138 m | 309 |
| 2.0 | 0.168 m | 305 |

つまり堰の正体は**境界粒子の斥力そのもの**。「壁を貫通させないこと」と
「リムで液体を通すこと」が同じパラメータの裏表になっている。

### 対策1（実施済み・部分的に有効）: リム開口での境界密度寄与のフェード

境界粒子の psi を、リム面から下へカーネル半径 1 個分の帯で smoothstep で 0 へ落とす
（`FluidBoundary.rimFadePerKernel`、既定 1.0）。壁の形も、リムから離れた場所の斥力も
変えない。あわせて SafetyCorrection をリムまで有効に戻した（開口端で壁が弱くなるため）。

62° 傾斜での実測:

| | 残存(24秒後) | 液面 - 堰 | リム開口の暴れ | 壁の貫通 |
|---|---|---|---|---|
| 対策前（このセッション開始時） | 90.5% | 0.157 m | 2.15 m/s | 465 |
| 粘性の dt 比例化のみ | 78.0% | 0.118 m | 2.27 m/s | 465 |
| **+ リムフェード 1.0** | **70.5%** | **0.093 m** | **0.29 m/s** | **0** |
| 理論値 | 41.7% | 0 m | — | 0 |

- フェード量は 1.0 で飽和する（1.6 / 2.2 でも 72.2% / 0.098m のまま）。
  つまり**堰のうち境界斥力に由来する分は取り切れた**。
- 残る 0.093 m ≒ 1.7h は境界起因ではない。
- 粘性は高いほど流出が良くなる（visc 0.6 → 76.3% / 1.4 → 74.4% / 2.8 → 72.4% / 5.0 → 70.6%）。
  低粘性だと粒状に振る舞って流れにくい。現状の 2.8 は妥当。
- 境界粘性を下げても効果は小さい（bvisc 0.15 で 71.6%）。

**OI-2（壁の貫通）はこの対策で解決した（465 → 0 個）。**
あわせて Overflow / 貫通の判定を「RimOpening 筒の通過」から「リム面の通過」へ変更し、
Inside 判定に粒子半径の許容幅を入れた。penetrationEvents 988 → 12、
分類カーネルの inside 数と CPU 幾何判定が完全一致するようになった。

### 決定的な切り分け: 開口箱では**逆に流れ過ぎる**

同じソルバー・同じパラメータのまま、容器だけを単純な開口箱
(1.0 x 1.2 x 1.0、スケール1、壺プロファイル無し = SafetyCorrection も無効) に
差し替えて同じ測定をした。

| 傾き | 実測の残存 | 理論の残存 | 液面 − 堰 |
|---|---|---|---|
| 30° | 100.0% | 100.0% | −0.563 m（こぼれない角度。正しい） |
| 55° | **54.6%** | 84.0% | **−0.150 m = −2.02 h** |
| 70° | **29.4%** | 43.7% | **−0.087 m = −1.17 h** |

**箱では堰が存在しないどころか、理論より流れ過ぎて液面が堰より 1〜2h 低い位置で止まる。**
（自由表面付近の粒子が飛び出しやすいので、平衡が堰より下に落ち着くのは妥当）

したがって:

- 残留堰は **PBF ソルバー一般の性質ではない**。自由表面の圧力欠損が原因なら
  箱でも同じ向きの堰が出るはずだが、出ない。むしろ逆向き。
- 原因は **壺モード固有**（内径プロファイル形状、リング状の境界粒子生成、
  プロファイル参照、あるいは壺モードでのみ有効な SafetyCorrection）に絞られた。

これは当初の最有力仮説「自由表面の圧力欠損」を**否定する**結果である。

### 残る 0.093m の堰について — 次に試す手（優先順・改訂）

1. **壺プロファイルを半径一定の円筒に置き換えて測る。**
   `FluidBoundary.debugForceCylinderRadius` を追加済み。
   円筒で堰が消えるなら原因は**内径形状（くびれ）**、残るなら**壺モードの境界構築**。
   ここで探索範囲が半分になる。
2. 円筒でも残る場合の候補:
   - 壺モードでのみ有効な `SafetyCorrection`（Box モードでは無効なので箱の結果には出ない）
   - リング状の境界粒子配置（箱は格子状。リングは方位角方向の間隔が半径で変わる）
   - `PotRadiusAt` の GPU 側 1D テクスチャ補間の誤差
3. 円筒で消える場合: 壺は belly 0.2306 → rim 0.1952 と **15% くびれている**。
   くびれた首では、越えるべき壁が液面より内側にせり出す。
   実際の壺の形なので「直す」対象ではなく、その分だけ残存が増えるのが正しい可能性もある。
   その場合は理論値の計算式を「くびれを考慮した実効堰」に改めるべきで、
   バグではなく**理論値側の誤り**ということになる。

### 旧・次に試す手（対策1 実施前）

1. **壺の実効内径を実測して profile を較正し直す。**
   境界粒子の斥力が profile より内側に実効内壁を作っているなら、
   静止時の液面高さと充填率の関係にズレが出るはず。まずそこを測る。
   （静止時の液面は目標と一致しているので、この線は弱いかもしれない）
2. **開口端付近だけ境界密度寄与の到達距離を短くする。**
   リムから下 h の帯で、境界粒子の密度寄与を距離で減衰させる。
   壁の法線方向の押し戻しは残す。**境界の扱いを変えるので §43 の報告対象。**
3. 粒子数を上げる（16384 → 65536）。堰が h に比例しないので効果は薄いと予想。

### 関連ファイル

- `Assets/Scripts/Fluid/FluidCore.cs` — `boundaryPressureScale`
- `Assets/Scripts/Fluid/FluidBoundary.cs` — `ComputeVolumes` / `BuildPot`
- `Assets/Shaders/Fluid/FluidCore.compute` — `ComputeDensityLambda` の境界寄与

---

## OI-2: 壁の貫通 — **解決済み**

**状態**: 解決（2026-08-13）

`boundaryPressureScale` 1.0 → 1.6 で 963 → 74 まで低下。
その後、OI-1 の対策1（リムフェード）に伴い SafetyCorrection の上限を
`RimY - h` から `RimY` へ戻したところ、62° 傾斜で **throughWall = 0 個**になった。
リム帯だけ SafetyCorrection が無効だったことが、残っていた貫通の原因だった。

---

## OI-4: 非同期リードバックにするとエディタが固まる

**状態**: 既定を同期に戻して回避中。原因未特定。
**影響度**: 中。§16 が指定する非同期リードバックが使えていない。

### 症状

`FluidCore.synchronousReadback = false`（§16 の非同期リードバック）にすると、
**Play 中に FluidCore を無効化→有効化しただけでエディタが固まる**。

最小再現:

```csharp
var core = Object.FindObjectOfType<FluidCore>();
core.enabled = false;   // ここで固まる
core.enabled = true;
```

非同期化する前は同じ操作が何度も通っていた（sweep スクリプトが多用していた）。
このセッションでエディタを 4 回強制終了する羽目になった原因。

### 試したが効かなかった対策

| 対策 | 結果 |
|---|---|
| `Release()` の先頭で `AsyncGPUReadback.WaitAllRequests()` | 固まる（この呼び出し自体がデッドロックする可能性もある） |
| カウンタのリングバッファを `Release()` で解放せず、コンポーネント寿命に合わせる | 固まる |

### 分かっていること

- 非同期化の効果は実測済み: `core.Step()` の CPU コストが 13.2ms → 0.12ms。
  GPU の仕事量は変わらないが、CPU が毎フレーム GPU を待たなくなる。
  ゲームでは意味のある差なので、直す価値はある。
- 現在は既定 `synchronousReadback = true` で回避しており、機能・品質への影響は無い
  （数値は 1 フレーム遅れずに読める）。コストは CPU が毎フレーム GPU を待つこと。

### 次に試すこと

1. `AsyncGPUReadback.Request` に `NativeArray` 版を使い、宛先を GraphicsBuffer から切り離す
2. リクエストごとに完了フラグを持ち、未完了のリクエストがある間は `Release()` を遅延させる
3. そもそも `Release()`/`Initialise()` を再初期化のたびに走らせない設計にする
   （バッファサイズが変わらないなら作り直す必要が無い）

---

## OI-5: 地面の液体がゴブリンの周囲しか描画されない

**状態**: 原因特定済み・未着手（OI-3 の Brick Pool で同時に解決する）
**影響度**: 中。こぼした跡が残らないので「こぼした」感が薄い。

### 原因

密度場は壺に追従する固定サイズの箱で、現在の半径は **約 1.77m**
（`lateralSpread = 0.8` + 壺の旋回半径 0.969）。この外にある液体は
粒子としては存在し Mass にも数えられているが、**密度場に入らないので描画されない**。

ゴブリンの歩行速度は 1.0 m/s なので、こぼした水たまりは **2 秒足らずで見えなくなる**。

### 選択肢

| 案 | 内容 | 見える半径 | VRAM | 品質 |
|---|---|---|---|---|
| A | `maxVoxelsPerAxis` 384 → 448、`lateralSpread` 0.8 → 1.73 | 2.7m | 約 514MB | 変わらず |
| B | Voxel を粗くして範囲を広げる | 任意 | 現状維持 | **低下（§36 で禁止）** |
| C | **Brick Pool (OI-3)** を実装 | 5m 以上 | 約 130MB | 変わらず or 向上 |

A は「2 秒 → 3.5 秒」程度の改善にしかならず、VRAM が 2.5 倍になる。
C が本命で、OI-3（Voxel 6mm 化）と同じ実装で同時に解決する。

---

## OI-3: 液柱が薄い半透明フィルムに見える / Voxel 解像度が足りない

**状態**: **大幅に改善（2026-08-13）**。扇状の筋・皺は解消した。残るのは薄さの表現。

### 解消した部分

扇状の筋と皺の正体は解像度ではなく **等値面の三角形の頂点が混ざるバグ**だった。
`AppendStructuredBuffer.Append()` を 1 三角形につき 3 回呼んでいたため、
他スレッドの Append が割り込んで別々の三角形の頂点が組み替えられていた。
三角形番号を 1 回の InterlockedAdd で取り、連続 3 スロットへ直接書くよう修正。
静止時の液面は継ぎ目・筋の無い滑らかな面になった。

### 残っている部分（以下は修正前の分析）

**旧状態**: 原因特定済み・未着手
**影響度**: 中。見た目の要件（不透明な濃緑）に届かず、Phase 8 の TEST K/L も阻害する。

### 症状

壺の中の液体は濃緑に見えるが、リムから出た液柱は淡い半透明のフィルムに見え、
表面に扇状の筋と皺状のファセットが出る。

### 切り分け済み

表面パラメータでは改善しない（Phase 8 で実測）:

| 変えたもの | 結果 |
|---|---|
| `smoothingPasses` 2 → 4 | 変化なし |
| `isoValue` 0.25 / 0.45 / 0.8 | 0.8 で細くなるが扇状の筋は悪化 |
| `normalEpsVoxels` 1.2 → 2.4 | 変化なし |

扇状の筋は、**薄いシートで等値面が閾値ぎりぎりになり、ほぼ退化した三角形が
大量に出る**ことによる。パラメータではなく解像度の問題。

### 必要な作業と見積もり

設計書 §22-A の調整順序は「1. Voxel Resolution」が最初。現状 9.4mm を 6mm 以下にしたい。

- 6mm にすると密度場は 1 軸 543 voxel 必要。現在の「密な 3D テクスチャ + 疎ディスパッチ」
  方式では **VRAM 約 1GB** で成立しない（現状 9.4mm で 31.6M voxel / 241MB）。
- §14 が本来定義している **Brick Pool**（間接参照つき、メモリが有効 Brick 数に比例）が必要。
- 実測の有効 Brick 数は 2700〜4300（全 63888〜69696 のうち 4〜6%）。
  プールを 32768 Brick 確保すれば十分で、VRAM は
  32768 x 512 voxel x (uint 4B + half 2B x 2) = **約 130MB** に収まる。
  つまり **6mm でも現状より軽くなる**。

### 実装で必要になるもの

1. `BrickSlot`（Brick index → プール slot、未割当は 0xFFFFFFFF）
2. `AllocateBricks` カーネル（atomic カウンタで slot を配る）
3. プール本体（uint 蓄積用 / float 復号用 ping-pong）
4. `SampleField(voxel)` ヘルパ（Brick 参照 → プール読み。未割当は 0）
   - Blur / Marching Cubes / 法線のトリリニア補間を全てこれ経由にする
5. **液体マテリアルの thickness march も間接参照に対応させる**
   （現在は `_DensityField` を 3D テクスチャとして直読みしている）

5 が見落としやすい。マテリアル側を変えないと、表面は精細になるのに厚みだけ
古い経路で壊れる。

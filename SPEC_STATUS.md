# 仕様書に対する実装状況 報告書

対象仕様: 「ポーション液体システム 完全自作・最終仕様」(§1〜§47) および
それを受けた設計書 `FLUID_DESIGN.md`
作成: 2026-08-13

判定の凡例:

- ✅ **達成** — 仕様どおり動作し、実測で確認済み
- ⚠️ **部分達成** — 動くが仕様の水準に届いていない。数値を併記
- ❌ **未達** — 実装できていない、または合格条件を満たさない
- 🔧 **仕様から変更** — 実装方針を変えた。理由を併記

---

## 1. 総括

| 区分 | 件数 | 内訳 |
|---|---|---|
| ✅ 達成 | 21 | 基盤・物理・領域分類・Mass 会計・ゴブリン統合など |
| ⚠️ 部分達成 | 3 | 傾斜時の流出量、表面の質感、性能目標 |
| ❌ 未達 | 2 | TEST K（液だれ）/ TEST L（液滴） |
| 🔧 変更 | 6 | 実装方式の変更。すべて理由と実測付き |

**構造は仕様どおり完成している。** 「Fluid Physics → Particle State → Density Field →
Surface Reconstruction → Liquid Surface → Liquid Material」の一本道が実装され、
Overflow・地面の液体・ゲージまで**すべて同じ粒子・同じ密度場・同じ等値面**から出ている。
代替表現（Sphere / Line / 専用 Mesh / Decal）は 1 つも使っていない。

**未達は 2 点に集約される。** どちらも根が同じで、
「リム開口に残る堰（OI-1）」が「少量の液体が細く垂れる」状態を作れなくしている。

---

## 2. 達成した項目

### 2.1 基盤 (§1〜§8)

| 項目 | 状態 | 実測・根拠 |
|---|---|---|
| 完全自作 / 有料 Asset 不使用 | ✅ | 流体コードは 100% first-party。参照は論文のアルゴリズムのみ（PBF: Macklin & Müller 2013 / Boundary・表面張力: Akinci et al. 2012, 2013）。`Assets/ThirdParty/` は不要 |
| GPU Compute による PBF | ✅ | `FluidCore.compute` 18 カーネル。16384 粒子 |
| World Gravity のみが外力 | ✅ | 疑似重力の経路はコード上存在しない。傾けた壺の中で液面が world 水平になることを実測（world Z で 12 分割、全区間 1.094 ±0.01） |
| Moving Boundary で容器の運動を伝える | ✅ | 境界粒子 12191 個。`V_b = v + ω×r` |
| 空間ハッシュ | 🔧 | 下記 3.1 |
| CFL サブステップ | ✅ | 2〜10 の適応。実測最大速度から決定 |
| 境界粘性（接線方向） | ✅ | 剛体回転比（90°/s・4秒後）: bv 0 → 0.029 / **0.55 → 0.844** / 2.8 → 0.900 |

### 2.2 壺内部境界 (§10)

| 項目 | 状態 | 実測 |
|---|---|---|
| 実測内径プロファイルによる境界粒子 | ✅ | FloorY 0.0456 / RimY 0.3601 / RimR 0.1952 / MaxRadius 0.2311 |
| Open Boundary（リムで打ち切る） | ✅ | リムより上の境界粒子 0 個 |
| 壁の貫通ゼロ | ✅ | 62° 傾斜で `throughWall = 0`、`penetrationEvents = 10〜12` |
| SafetyCorrection が常態化しない | ✅ | 直近1秒あたり数個。位置差は速度に変換されない（`Finalize` で `ComputeVelocity` の後） |

### 2.3 密度場・表面 (§12〜§14)

| 項目 | 状態 | 実測 |
|---|---|---|
| 粒子を描かない | ✅ | 描画は等値面のみ。`FluidDebugView` は既定 OFF |
| uint 固定小数の atomic 蓄積 → float 分離 | ✅ | 仕様どおり |
| 壺内・Overflow・地面が同一の密度場 | ✅ | 場は 1 つ。分割していない |
| 等値面抽出 | 🔧 | 下記 3.2 |
| Sparse Brick | ✅ | 有効 Brick 2668〜4319 / 全 63888〜69696（4〜6%）。表面 0.8〜1.0 ms/frame |
| 不透明な緑（膜に見えない） | ⚠️ | 壺の中は濃緑。液柱は薄い半透明に見える（OI-3） |

### 2.4 Overflow・領域分類 (§11)

| 項目 | 状態 | 実測 |
|---|---|---|
| Overflow は同じ粒子（生成しない） | ✅ | リムから地面まで連続した液柱。新規生成コードは存在しない |
| 分類カーネルが液体を押し出さない | ✅ | 位置・速度は SRV としてしかバインドしていない。構造上書き込めない |
| Inside / RimOpening / Airborne / Ground | ✅ | 分類カーネルの inside 数と CPU 幾何判定が完全一致（12259 = 12259） |
| 正常 Overflow と貫通の区別 | ✅ | Pour 3.5秒で 4408 / 12 |

### 2.5 Mass 会計・PotionVolume (§16, §17)

| 項目 | 状態 | 実測 |
|---|---|---|
| TotalMass = Pot + Airborne + Ground + Retired が一定 | ✅ | 17.4 秒の全サンプルで 0.2554 のまま。**収支誤差 0** |
| Retired へ移した Mass は戻らない | ✅ | 単調増加を確認 |
| PotionVolume = PotMass / InitialTotalMass | ✅ | 一方向。液面から逆算する経路は存在しない |
| ゲージ連動 | ✅ | `IPotionVolumeSource` 経由。Game View で追従を確認 |

### 2.6 Ground Fluid (§20)

| 項目 | 状態 | 実測 |
|---|---|---|
| 地面到達後も流体のまま | ✅ | 同じ密度場・同じ等値面に参加。Decal ではない |
| 一定時間後に Retired | ✅ | `groundLifetime = 8s`。着地 約3秒 + 8秒 ≒ 11秒から Retired が出始める |

### 2.7 ゴブリン統合 (§21)

| 項目 | 状態 | 実測 |
|---|---|---|
| CarryRig の LateUpdate 後の姿勢を読む | ✅ | 実行順 GoblinCarryRig(0) → FluidCore(100) → FluidSurface(200) |
| 容器の線速度・角速度 | ✅ | Transform 差分 |
| テレポート検出 | ✅ | 下記 3.5（新規実装） |
| ゴブリンの姿勢から液面角度を決めない | ✅ | そのようなコードは存在しない |
| Ground Slope | ❌ | 未実装。城の床が平面のため未着手。§19 により用途は Ground Fluid の流動方向のみ |

---

## 3. 仕様から変更した点と理由

### 3.1 🔧 Bitonic Sort → 計数ソート（一様グリッド）

**仕様**: §5/§7 で Bitonic Sort を想定。
**変更**: 計数ソート（Clear → Count → Scan → Scatter）。
**理由**: 16384+12191 粒子の Bitonic Sort は 105 ディスパッチ必要。計数ソートは 7 ディスパッチ。
どちらも**セルあたりの上限が無い完全ソート**で、近傍探索の正しさは同一。
性能目標（§23）の「Bitonic Sort 0.2 ms」は、より速い方式で置き換えた形。

### 3.2 🔧 256 ケースの Marching Cubes テーブル → 四面体分解

**仕様**: §13「GPU Marching Cubes」。
**変更**: 立方体を 6 個の四面体へ分解し、四面体ごとに「内側頂点が何個か」から
一次原理で三角形を導出。256 ケースのテーブルを持たない。
**理由**: ライセンス上の理由。公開されている MC テーブルは出典が曖昧なものが多く、
§41 の「ライセンスが不明なコードは使用しない」に抵触しかねない。
記憶から書き起こすと誤りが混入する危険もある。
四面体分解は同じ等値面を生成し、watertight である
（隣接四面体が共有面で必ず同じ辺補間点を作るため）。
全四面体が対角 0-6 を共有する分解にして、隣接セル間でも継ぎ目が出ないようにした。

### 3.3 🔧 粘性のブレンド率を dt 比例にした

**仕様**: §9 に係数の定義はあるが、時間刻みとの関係は未定義。
**変更**: `ブレンド率 = 係数 × dt / 基準時間刻み(1/60)`。
**理由**: XSPH 粘性は「周囲の速度へ何%寄せるか」というブレンドで、**サブステップごとに**
適用される。サブステップ数は CFL で 2〜10 に変動するため、**容器を速く動かすほど
中身が粘る**という物理的にあり得ない挙動になっていた。
さらに 10 サブステップでは壁際の速度が 1 フレームで 0.45^10 ≒ 0.0003 倍になり、
リムを越える流出層（厚み ≒ h）ごと壁に貼り付いて液体が溢れ出せなかった。
実測: この修正で 62° 傾斜の残存が 90.5% → 78.0% に改善。
係数は同じ効きになる値に取り直した（`viscosity` 0.28 → 2.8）。

### 3.4 🔧 境界の psi をリム開口でフェードさせた

**仕様**: §10 は Akinci の ψ = 1/ΣW をそのまま使う想定。
**変更**: リム面から下へカーネル半径 1 個分の帯で ψ を smoothstep で 0 に落とす。
**理由**: 壁は密度計算に参加することで斥力を生むが、**開口部でもそれが働く**ため、
リムを越えようとする液体が持ち上げられ、実際のリムより高い堰ができていた。
堰の高さが `boundaryPressureScale` に正比例することを実測で確認
（1.0 → 0.124m / 1.6 → 0.138m / 2.0 → 0.168m）。
実測: 残存 78.0% → 70.5%、液面−堰 0.118 → 0.093 m、リム開口の暴れ 2.27 → 0.29 m/s、
**壁の貫通 465 → 0 個**。

### 3.5 🔧 テレポート検出を追加（仕様の未実装項目）

**仕様**: §21 の表に「平滑化・テレポート検出」とあるが、設計書に具体化されていなかった。
**追加**: 1 フレームの移動が 12 m/s / 900 deg/s を超えたらテレポートとみなし、
速度としては扱わず、中身を同じ剛体変換で連れて行く。
**理由**: ゲーム開始直後、`Carry_Pot` がシリアライズ位置 (y=1.173) から
GoblinCarryRig が計算する手の中央 (y=1.606) へ 1 フレームで移動し、
その差分が境界速度 **26 m/s** として入って液体が飛び散っていた。
中身を連れて行かないと液体だけ取り残され、次のフレームには「壺の外」＝全量こぼれた判定になる。
実測: 静止時 PotionVolume 0.857 → **1.000**。

### 3.6 🔧 領域外へ出た粒子を Retired にする（仕様の追加）

**仕様**: §16 に「シミュレーション範囲外」の扱いは無い。
**追加**: 領域の水平端に達した粒子を Retired にする。
**理由**: ゴブリンが歩くと計算領域が一緒に動くため、地面の水たまりが
見えない壁に沿って引きずられて付いてきた。壺の中の液体がそこへ到達することはないので、
対象は既にこぼれた液体だけ。Mass は RetiredMass へ移るので収支は保たれる。

---

## 4. 未達の項目

### 4.1 ❌ TEST K（液だれ）/ TEST L（液滴） — §22-A の最重要 Visual Validation

**合格条件**: リムから連続して伸びる → 根元が太い → 下ほど細くなる → Neck → 液滴が分離。

**実測**:

| ケース | 結果 |
|---|---|
| 55° までゆっくり傾けて保持 | 液体がリムに張り付いたまま**落ちない**。ground 0 個 |
| 62° で 1.8 秒注いでから戻す | **16384 個中 16384 個が壺内へ戻った**。リムに残る液が無い |
| 62° 保持 | 太いシート状。Neck も液滴も生じない |

**原因**: 「少量の液体が細く垂れる」状態そのものが作れない。
液体は「全く出ない」か「シートで大量に出る」かの二択になっている。
根は 4.2 の残留堰。

**解像度も不足**している。表面パラメータ（smoothing 2→4 / iso 0.25・0.45・0.8 /
normalEps 1.2→2.4）を振ったが、薄い部分の扇状の筋は改善しなかった。
薄いシートで等値面が閾値ぎりぎりになり退化三角形が出るためで、
§22-A の調整順序どおり「1. Voxel Resolution」から手を付ける必要がある。

### 4.2 ⚠️ 傾斜時の流出量が理論の半分（OI-1）

| 傾き | 実測の残存 | 理論の残存 |
|---|---|---|
| 62° | 70.5%（24秒で頭打ち） | 41.7% |
| 80° | 36.8% | 12.4% |
| 100° | 11.8% | 0.2% |

液面が最低リム点より **+0.093 m** 高いところで静止する。

**測定が正しいことの確認**: 「壺内粒子数 × 粒子体積」から逆算した液面 1.073 と、
粒子分布から見た液面 1.079 が一致。本当に堰より上まで詰まっている。
バルクは完全に静定（0.019 m/s、ρ/ρ0 = 1.019）しており、暴れて盛り上がっているのではない。

**否定できた原因**（すべて実測）: 表面張力 / Akinci ψ の開口端発散 /
SafetyCorrection / リム上端シェルの棚 / Sim 領域の壁 / 圧力ソルバー強度 /
カーネル半径 / 人工圧力 / 自由表面の λ 下限。

**決定的な切り分け**: 容器を単純な開口箱に差し替えると、**堰が出ないどころか
理論より流れ過ぎる**（55° で実測 54.6% / 理論 84.0%、液面が堰より 2.02h 下）。
したがって残留堰は **PBF ソルバー一般の性質ではなく壺モード固有**である。
当初の最有力仮説「自由表面の圧力欠損」はこれで否定された。

**残る候補**: ①内径のくびれ（belly 0.2306 → rim 0.1952、15%） ②リング状の境界粒子生成
③壺モードでのみ有効な SafetyCorrection ④プロファイル参照。
切り分け用に `FluidBoundary.debugForceCylinderRadius`（半径一定の円筒に置換）を実装済みだが、
4.4 のエディタ停止に阻まれて未実行。

**なお①が原因なら「直す対象ではない」可能性がある。** 理論値は「リム円の最低点を通る
水平面より下の容積」で計算しているが、くびれた首では越えるべき壁が液面より内側に
せり出すため、実効堰はリムより高くなる。その場合、誤っているのは理論値の式である。

**ゲームプレイへの影響は限定的**: 実測で、ゲーム中の壺の傾きは `armBalance` ±1.0 で
**±10 度が上限**。45% 充填の壺は 10 度ではこぼれない。実際の「こぼれ」は
よろけ（GoblinStagger）による揺さぶりで起きており、そちらは正しく動いている
（PotionVolume 1.000 → 0.816 / 7秒）。

### 4.3 ⚠️ 性能が目標に届かない（§23）

| 処理 | 目標 | 実測 |
|---|---|---|
| PBF | 0.5〜1.5 ms | **10.96 ms**（6 サブステップ） |
| ソート | 0.2 ms | （PBF に含む） |
| 密度場 + 平滑化 + MC + 描画 | 1.8〜3.5 ms | **0.84 ms** ✅ |
| **合計** | **2.5〜4 ms** | **11.8 ms** |

表面側は Sparse Brick により目標を大きく下回った。物理が目標の 7〜20 倍。
§36 に従い品質を落とす最適化はしていない。実施した最適化は 2 件:

- CFL に実測速度を使う（従来は速度クランプ値 8 m/s を使っていたため、静止時でも
  常に最大サブステップに張り付いていた）。品質を保てる下限を実測して `minSubSteps = 6` を採用
- 領域カウンタのリングバッファ化

残る候補（未実施、いずれも作り替えが必要）: 境界粒子 12191 個を毎サブステップ
world 変換 + ソートし直している（全体の 43%）/ 近傍探索が必要な球体積の約 6.4 倍を走査。

### 4.4 ❌ 非同期リードバックが使えない（OI-4、新規発覚）

**仕様**: §16「集計は GPU の InterlockedAdd（固定小数）→ **非同期リードバック**」。
**状態**: 実装したが、**Play 中に FluidCore を無効化→有効化しただけでエディタが固まる**。
最小再現は `core.enabled = false;` の 1 行。同期に戻すと 40 ms で通る。
このセッションで Unity を 4 回強制終了する原因になったため、既定を同期に戻した。

機能・品質への影響は無い（数値は 1 フレーム遅れずに読める）。
コストは CPU が毎フレーム GPU を待つこと（実測 13.2 ms の CPU 待ち）。

---

## 5. 未検証の項目

| 項目 | 理由 |
|---|---|
| TEST G 坂道 / H ジャンプ / I 着地 | ゴブリン側の該当挙動が未実装または未確認 |
| TEST M Ground Flow の広がり方 | 地面到達は確認済みだが、広がり方の合格判定は未実施 |
| TEST O Low Volume | PotionVolume は動くが、低残量時の見た目判定は未実施 |
| Ground Slope | 城の床が平面のため未着手 |
| 円筒テスト（OI-1 の切り分け） | 4.4 のエディタ停止に阻まれた |

---

## 6. 対策方針の候補（判断材料）

優先順は未定。判断のための材料として並べる。

| # | 対策 | 期待効果 | 規模 | リスク |
|---|---|---|---|---|
| A | 円筒テストで OI-1 の原因を確定 | 探索範囲が半分になる。「実は理論値の誤り」だった場合はここで終わる | 小（実装済み、実行のみ） | 低 |
| B | Brick Pool を実装して Voxel 6mm 以下に | TEST K/L の前提。表面の質感（OI-3）も解決。**VRAM は 241MB → 約130MB と軽くなる** | 大（compute + C# + マテリアル） | 中（マテリアルの thickness march も間接参照化が必要） |
| C | OI-4（非同期リードバック）の原因特定 | CPU の毎フレーム待ち 13.2 ms を消せる | 中 | 中（再びエディタを固める危険） |
| D | 物理の最適化（境界粒子の再ソート削減など） | 10.96 ms の削減 | 中〜大 | 中 |
| E | 現状で凍結し、ゲーム側を進める | 追加コスト無し。ゲームループは既に成立している | なし | 液だれ・液滴は出ないまま |

**補足**: A は B の前に済ませる価値がある。OI-1 が「理論値の誤り」だった場合、
TEST K/L の失敗原因は解像度だけになり、B が唯一の対策になる。
逆に OI-1 が実バグなら、B の前に直さないと液だれの形状が正しく出ない。
